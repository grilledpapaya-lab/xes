# Personal index

Static landing page. Enable GitHub Pages: Settings → Pages → Deploy from branch `main`, folder `/ (root)`.

Edit the `SITE` object in `index.html` to put your name, email, and work in.
